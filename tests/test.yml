# test file for fail2ban
---
- hosts: localhost
  remote_user: root
  roles:
    - { role: ansible-etckeeper, install: true }
    - { role: ansible-etckeeper, etckeeper_message: 'Ansible changes during Travis test' }
